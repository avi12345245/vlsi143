Here we have implemented a 4:1 MUX in the verilog using the case statement procedural assignment block, by using the 
select line variable as the case variable and then assigning inputs to y. A point to be added is that for more complex
circuits add default statemnets also for 'dont care' and 'Z' cases.o
